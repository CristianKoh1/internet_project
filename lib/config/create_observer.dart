

import 'package:flutter/widgets.dart';

List<NavigatorObserver> createNavigatorObservers() {
/*   final observer = MyObserver();
  final singularObserver = SingularObserver();
  final _analytics = getIt<IFirebaseAnalyticsRepository>();
  List<NavigatorObserver> observers = [
    observer,
    singularObserver,
  ];

  final firebaseObserver = FirebaseAnalyticsObserver(
    analytics: _analytics.analytics,
  );

  observers.add(firebaseObserver);

  return observers; */
  return [];
}